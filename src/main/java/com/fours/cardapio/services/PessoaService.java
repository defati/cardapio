package com.fours.cardapio.services;
/*
 * package com.fours.cardapio.service;
 * 
 * import java.util.List; import java.util.Optional;
 * 
 * import org.springframework.beans.factory.annotation.Autowired; import
 * org.springframework.http.ResponseEntity; import
 * org.springframework.stereotype.Service;
 * 
 * import com.fours.cardapio.entities.Pessoa; import
 * com.cardapio.repository.PessoaRepository;
 * 
 * 
 * @Service public class PessoaService {
 * 
 * @Autowired PessoaRepository pessoaRepository;
 * 
 * public ResponseEntity<String> salva(Pessoa pessoa){
 * pessoaRepository.save(pessoa); return
 * ResponseEntity.status(201).body("Salvo com sucesso"); }
 * 
 * public ResponseEntity<List<Pessoa>> lista(){ return
 * ResponseEntity.ok(pessoaRepository.findAll()); }
 * 
 * public ResponseEntity<Optional<Pessoa>> buscaPorId(Long id){ return
 * ResponseEntity.ok(pessoaRepository.findById(id)); }
 * 
 * public ResponseEntity<List<Pessoa>> pesquisa(String pesquisa){ return
 * ResponseEntity.ok(pessoaRepository.findPessoaByNomeContaining(pesquisa)); }
 * 
 * public ResponseEntity<String> edita(Pessoa pessoa){
 * pessoaRepository.save(pessoa); return
 * ResponseEntity.status(200).body("Editado com sucesso"); }
 * 
 * public ResponseEntity<String> deleta(Long id){
 * pessoaRepository.deleteById(id); return
 * ResponseEntity.status(200).body("Deletado com sucesso"); }
 * 
 * }
 */